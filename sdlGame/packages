# All plugins are given as a relative path from $(./path)
# The next commented lines are examples of how not to add a plugin:
# /home/michael/sdlGame/test.so # comments can't be at the end of a line
#	l /home/michael/sdlGame/overworld.so # the lead whitspace is OK, but the l is blocking this line from loading
lvl1.so
overworld.so
test.so
